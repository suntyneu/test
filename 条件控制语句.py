"""
if 语句

if-else语句

if-elif-else语句
格式：
if 表达式1：
    语句1
elif 表达式2：
    语句2
...
elif 表达式n:
    语句n
else:       (可有可无)
    语句

逻辑：当程序执行到if-elif-else语句时，首先计算表达式1的值，如果表达式1的值为真，则执行语句1；
执行完语句1，则跳过整个if-elif-else循环；如果表达式1的值为假，计算表达式2的值，为真则执行语句2，
执行完语句2，跳出整个if-elif-else循环，如果为假则计算表达式3的值......
如果表达式没有1个真的，切有else，则执行else语句。

每个elif都是对它上面表达式的否定

"""

